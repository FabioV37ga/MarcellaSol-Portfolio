// import html from "nanohtml"

export interface system{
    base: HTMLElement;
    home: HTMLElement;
    client: HTMLElement
    // home2: HTMLElement
    // test: HTMLElement
}

export interface dbView{
    _id: string;
    viewName: string;
    permission: string;
    view: string;
}

