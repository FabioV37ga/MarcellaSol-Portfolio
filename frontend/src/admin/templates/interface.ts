// import html from "nanohtml"

export interface system{
    home: HTMLElement;
    test: HTMLElement
}

export interface dbView{
    _id: string;
    viewName: string;
    permission: string;
    view: string;
}

